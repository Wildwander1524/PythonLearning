# Learning Record — Week 1 Baseline

> **Date:** 2026-06-18 · **Author:** Claude (from the user's actual D1–D2 submissions)
> **Purpose:** the starting snapshot of mastery, so any future session knows *exactly* where
> you are and what to re-test. New records are appended as dated files in this folder; each
> day add 3 lines (easy / hard / re-test). The `/teach` method reads these to schedule spacing.

---

## How to write your daily record (copy this stub)

```markdown
# Learning Record — Day 1 (<class and property>)
**Date:** 2026-06-18
- ✅ Came easily: almost except history copy-on-read
- 🧩 Was hard / had to look up: implementing the `Temperature` class with read-only properties
- 🔁 Re-test me on this later: none
- ⏱️ Fluency vs storage: (could I do tomorrow's cold RECALL? guess y/n) y
```

---

## Baseline snapshot (from submitted code)

### Day 1 — `BankAccount` · **9/10 tests** · verdict 🟢
- ✅ **Strong:** encapsulation (read-only `balance` property, no setter), all 4 validation
  cases, copy-on-read `history()`, class-attribute counter via `BankAccount.account_count`,
  clean test design.
- 🧩 **Slipped:** `transfer()` tested but never implemented (left a **red test** — see E4);
  history stored as a tuple grown with `+=` (O(n²) — see E4); redundant `amount == 0 or
  amount < 0`; dead `pass` after `return`; `__str__` not formatted to 2 dp.

### Day 2 — `animals.py` + `shapes.py` · **18/22 tests** · verdict 🟡
- ✅ **Strong:** `super()` in every subclass; `Cat.speak` EXTEND flavor; `Parrot` MRO chain +
  both `isinstance` checks; `Shape` ABC + inherited `describe()`; `total_area` with **zero
  `isinstance`** (textbook polymorphism); triangle-inequality validation; stretch goals
  (`SwimMixin`, `Square`, `Penguin`) all correct; concept-check answers essentially right.
- 🧩 **Slipped (the 4 fails + 1 subtlety):**
  - `Circle.area` = `πr²/2` (semicircle — dropped a factor) → **E2 formula correctness**.
  - `Triangle.area` used `math.hypot` instead of **Heron's formula** → **E2**.
  - `Dog.fetch` ignored its `item` parameter → **E1 spec precision**.
  - `Parrot.__init__` used `super(Bird, self).__init__(...)`, **skipping `Bird` in the MRO**
    (worked only because `Animal` still set `sound`) → **E3 inheritance subtlety**.

---

## The 4 tracked growth edges (these drive homework difficulty & spaced review)

| Edge | One-line | Status | Closes when |
|------|----------|--------|-------------|
| **E1 — Spec precision** | code exactly what the spec says; use every named param | 🔴 open | 2 specs nailed first-try, spaced |
| **E2 — Formula/units correctness** | derive the formula, sanity-check units/factors before coding | 🔴 open | Heron + one new formula correct cold |
| **E3 — MRO/inheritance subtlety** | predict `__mro__`; know `super()` ≠ "the parent" | 🟠 partial | predict a 3-level MRO twice, spaced |
| **E4 — Pythonic polish & test hygiene** | right container, no dead code, never leave a red test | 🟠 partial | a full day green + clean, unprompted |

> Graduation rule: an edge turns ✅ only after it's retrieved correctly **after a gap** (not
> the same day you learned it). That's the fluency-vs-storage-strength line.

---

## Next session pickup
- Start the week at [`../MISSION.md`](../MISSION.md), then `day01_bankaccount/LESSON.md`.
- First spaced check is in `day02_inheritance/RECALL.md` (re-tests D1 + the E-edges above).
