# 2026-06-25 — Teach track: Day-5 (errors / context managers / exception hierarchy)

**Topic:** Week-1 Day-5 — exceptions, custom exception hierarchy, context managers (+ modules, linear combinations/span).
**Workspace:** `E:\Code\MyProjects\PythonLearning\week01\day05_errors_context`
**Status:** 🟡 In progress — session opened, cold checks done, traps logged. Lesson exists & is solid; user about to work through it.

> NOTE: This is the **/teach** track. Separate parallel **Hermes/env** track lives in
> `2026-06-24_HANDOFF.md` — do not conflate.

## Done this session (2026-06-25)
1. **3 overdue cold checks** (last seen 06-21) graded at session start, before new material:
   - Generator = `yield`-fn → `StopIteration` — **q2** (named but *searched*, no unaided recall). Reset 1d, due 06-26.
   - Matrix × vector → `[[6],[15]]` — **q2** (values right but *searched* + structure scaffolded). Reset 1d, due 06-26.
   - Two ways to make `for` work (`__iter__`/`__getitem__`, `IndexError` stop) — **q4 graduated**. 2d→6d, due 07-01.
   - User was honest about searching → kept spacing data accurate (no penalty for honesty; q reflects retrieval).
   - `review-queue.md` updated accordingly.
2. **`misconceptions.md`** — added Day-5 canonical traps **M14–M17** (rule #4, before RECALL):
   - M14 broad/bare `except` (E4) · M15 except-block ordering specific→general (E3) ·
     M16 custom exc must subclass `Exception` not `BaseException` (E3) · M17 `with` doesn't suppress
     unless `__exit__` returns truthy; runs guaranteed like `finally` (E4).
3. Confirmed **Day-5 lesson already built** (`LESSON.bilingual.md`, redesigned bilingual style, 21KB):
   LBYL/EAFP, try/except/else/finally, exception hierarchy + custom exceptions carrying data,
   context managers (`__enter__`/`__exit__`, suppress table), modules/packages/`__name__`,
   math = linear combinations & span. HOMEWORK = context-managed `FileLogger` as importable `logkit` pkg.

## Lesson checkpoints — ✅ both cleared (2026-06-25)
- **✍️ Self-explain** (is-a vs has-a): solid — got substitutability (`InsufficientFundsError` usable
  anywhere `BankError` is caught) vs `Stack` private has-a. Sharpened wording, banked.
- **🔮 Predict span**: ① `5·[1,0]+7·[0,1]` ✓ · ② `[3,4]∉span([1,1])` ✓ · ③ `[1,1],[2,2]` span plane?
  first said *yes* → after one nudge corrected: `[2,2]=2·[1,1]`, dependent, only the diagonal. ✓

## Next steps
2. Then HOMEWORK (`logkit` FileLogger — prove it closes the file *even on error*). Run tests in the
   **`dl`** conda env (Python 3.12.13), NOT base Anaconda 3.8.8.
3. Day-5 RECALL cold the following morning; grade with hint-ladder; update queue.
4. Due 06-26: re-test Generator/StopIteration + Matrix×vector COLD (no searching).

## Workflow update — new official plugins (2026-06-25)
User installed 4 official plugins (claude-code-setup, **pyright-lsp**, claude-md-management,
**frontend-design**) and asked to wire the workflow. User chose to integrate **pyright → Python
learning** and **frontend-design → /teach HTML** (declined global-doc / custom-skills edits).
- Created **`E:\Code\MyProjects\PythonLearning\CLAUDE.md`** (project-scoped): pyright type-diagnostics
  gate in the homework Definition of Done (edge E4 now machine-checked); `dl` env note; /teach viz ref.
- Edited **`/teach` SKILL.md** *Visualization Upgrades* → consult **frontend-design** skill before
  authoring lesson HTML.
- **Gap found + fixed:** pyright-lsp plugin was installed but `pyright-langserver` was **not on PATH**
  (LSP tool errored, LSP MCP server crashed/disconnected). Ran `npm install -g pyright` → **pyright
  1.1.411** at `C:\Users\Admin\AppData\Roaming\npm\` (on User PATH ✓). Verified it flags type errors.
- **Action pending (user):** run `/reload-plugins` (or restart) so the pyright LSP server reconnects
  now that the binary exists. Then `mcp__ide__getDiagnostics` + `LSP` give live pyright feedback in grading.
- Minor: pyright landed in npm-global (AppData), not the D:-drive convention — acceptable for a PATH
  langserver; relocate only if user wants (would need `npm config set prefix` to a D: path).

## Automations built (2026-06-25, user: "do them")
Built all 3 recommended automations + validated:
1. **`grade-day` skill** — `.claude/skills/grade-day/SKILL.md` (user-only) + `run_checks.sh`
   (pyright over the day + pytest in `dl`). Encodes DoD gate + hint-ladder + queue/misconceptions update.
2. **`lesson-html-reviewer` subagent** — `.claude/agents/lesson-html-reviewer.md` (Read/Grep/Glob):
   structural integrity, self-containment, JS/library API correctness, /teach contract, frontend-design a11y.
3. **context7 MCP** — added user scope (`claude mcp add context7 -s user -- npx -y @upstash/context7-mcp`),
   written to `C:\Users\Admin\.claude.json`. **Needs Claude restart to go live this session.** Use it
   for authoritative library docs once the roadmap hits numpy/pandas/PyTorch.

### Setup gaps found + fixed while validating grade-day
- **pytest was NOT in `dl`** — it lived in **base (now Python 3.13.9, upgraded from the old 3.8.8)**.
  The "use dl 3.12.13" convention was stale. Fixed: `pip install pytest` into `dl` → **pytest 9.1.1 on
  3.12.13**. Convention is now actually true.
- Added **`pyrightconfig.json`** (repo root) → points pyright at the `dl` venv (`venvPath` D:/ANACONDA/envs,
  `venv` dl, pythonVersion 3.12) so imports resolve and checks match the learning interpreter.

### ⚠️ grade-day's first real run caught a regression in "done" Day 4
Running in the correct `dl` env, **Day 4 banking.py is actually 10/12, not 12/12** (was reported green):
- `banking.py:185` — `sef.get(to_owner)` — **typo for `self`** → `NameError` fails BOTH transfer tests
  (`test_bank_transfer_moves_money`, `test_bank_transfer_overdraft_raises`).
- `banking.py:167` — `self.ledger.entries()` — **should be `self._ledger`** (statement() latent bug, untested).
- `banking.py:156/164` — `Ledger.record(amount: float, ...)` called with formatted **strings** (type mismatch, E4).
- RESOLVED (user chose "just fix it for me"): patched `banking.py` — `sef`→`self`, `self.ledger`→
  `self._ledger`, `record()` now passes floats (`amount`, `-amount`, `self._balance`), `transfer()`
  given `src`/`dst` Optional-narrow, dropped unused `field` import. **banking.py pyright-clean.**

## Env decision REVERSED → base (user, 2026-06-25)
User switched: run tests in **base (Python 3.13.9, pytest 8.4.2)**, repoint pyright to base.
- `run_checks.sh` → `conda run -n base`.
- `pyrightconfig.json` → base (`venvPath: D:/`, `venv: ANACONDA`, pythonVersion 3.13) — file kept, retargeted.
- project `CLAUDE.md` env note → base; retired the stale "use dl not base 3.8.8" rule (base is 3.13.9 now).
- (pytest also installed in dl earlier — harmless leftover; base is canonical.)
- Test-file pyright noise (read-only negative test + Optional access) → user chose **suppress**: added
  `# pyright: ignore[reportAttributeAccessIssue]` on the read-only line; narrowed `bank.get(...)` via
  `alice/bob is not None` asserts.
- **FINAL: `grade-day week01/day04_composition` → CHECKS PASS (pyright clean + 12/12).** Day 4 genuinely done.

## grade-day gate scope (user pref, 2026-06-25)
"Gate homework only next time, except when the test is part of the homework."
- `run_checks.sh` default now type-gates **homework code only** (`test_*.py` excluded); `pytest` still
  executes all tests. New **`--gate-tests`** flag includes test files in the pyright gate.
- Use `--gate-tests` when writing tests IS the deliverable → **Day 5 (Part C writes `test_logkit.py`) qualifies.**
- SKILL.md step 2 updated to match; also fixed stale `dl`/3.12.13 text → `base`/3.13.9 in the skill doc.

## Tooling-commit follow-up (2026-06-25, session 2 — "focus on tooling commit")
Reviewed all 6 files in the unpushed tooling commit `a42707b`. Tooling is sound; found **one stale
reference** it introduced:
- **`.claude/skills/grade-day/SKILL.md` line 3 (frontmatter `description`)** still said *"pytest green
  in the **dl** conda env"* — but §2 body + `run_checks.sh` both correctly use `conda run -n base`
  (3.13.9). The description is the skill's retrieval index, so it must match. **FIXED** → "base conda
  env (Python 3.13.9)". Working-tree diff is exactly that one line.
- Other `dl`/`ml` hits verified **legitimate** (PyTorch envs for Wk2/3 in RESOURCES.md/day03; CLAUDE.md
  line 17 correctly describes the *retired* convention). Only the frontmatter was wrong.
- **Pending user decision:** commit the fix as amend of `a42707b` (unpushed, fixes its own defect →
  clean) vs. new commit; then whether to `git push`.

## Open / housekeeping
- Day-4 work still **uncommitted** (`banking.py`, `LESSON.interactive.html`, queue, misconceptions). User hasn't asked to commit.
- Naming-convention note: handoff settled lessons → `./lessons/NNNN-name.html`; the existing per-day
  `*.bilingual.md` + `LESSON.interactive.html` predate that. Not migrating now (avoid churn).
