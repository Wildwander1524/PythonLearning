# HANDOFF — 2026-06-29 (rev 2) · Week-1 Day-5 done → RECALL next

> `/handoff` snapshot for a fresh agent. Saved to `progress/` (project convention overrides the skill's
> "OS temp" default) so this repo's SessionStart hook surfaces it as the resume point. **Supersedes the
> rev-1 of this file** (which said "logkit unbuilt" — now shipped). Blow-by-blow lives in
> [`progress/2026-06-29_day05-homework-and-retrieval.md`](2026-06-29_day05-homework-and-retrieval.md);
> this is the compact "where we are / what's next."

## Status: 🟢 Day-5 retrieval sweep + homework BOTH done (homework **assisted**) · ⏭️ next = **cold RECALL**

Nothing this session is committed yet. Branch is **1 ahead** of origin (old `af70a1e`, still unpushed —
learner chose no-push). Uncommitted: `week01/day05_errors_context/Code_file/` (the homework), both
`progress/2026-06-29_*.md`, and edits to `week01/{review-queue,misconceptions}.md`.

## What's DONE this session (don't redo)
1. **Overdue retrieval sweep — 6 items cold** (session-start discipline). Grades in `review-queue.md`
   under the `2026-06-29` log block: mutable-default *when* **q4**✅, `__eq__/__hash__` invariant **q4**✅,
   matrix×vector **q3**, iterable/iterator/generator **q3**, `__add__`-why **q3**, fail-fast **q2 (searched)**.
   Persistent slip = **`__add__`-why ↔ hash-contract cross-wire** (keeps recurring).
2. **Day-5 homework `logkit` — SHIPPED, gate GREEN.** `grade-day --gate-tests` → pyright **0/0**, pytest
   **5/5**; `python main.py` writes `app.log` + catches `LogLevelError`. Lives in
   `week01/day05_errors_context/Code_file/` (`logkit/{errors,loggers,__init__}.py`, `main.py`,
   `test_logkit.py`, `.gitignore`). NOTE filenames: logger module is **`loggers.py`** (not `logger.py`);
   package is one level deep under `Code_file/`.
3. **Homework was ASSISTED** — learner explicitly said *"check it, fix it and comment for me,"* so I handed a
   commented reference impl (stepped out of hint-ladder for that one turn). **Unaided (theirs):** ctx-mgr core
   (`__enter__`→self / `__exit__` close-always + `return False`) + exception hierarchy. **I fixed:** None-sentinel
   (`is not None`, not `or`/`bool()`), type hints, `pytest.raises` idiom.
4. **Bookkeeping:** added **M18** (None-sentinel truthiness trap) to `misconceptions.md`; `review-queue.md`
   "Awaiting first exposure" lists the D5 concepts as **ASSISTED → grade cold at RECALL** (None-sentinel = high priority).

## What's NEXT (in order)
1. **Day-5 RECALL, cold** — `week01/day05_errors_context/RECALL.bilingual.md`. **The real grade** for the 3
   assisted concepts: especially **None-sentinel (`is not None`)** and the **`pytest.raises`** idiom; also
   confirm ctx-mgr protocol + exception-hierarchy cold. Then update `review-queue.md` / `misconceptions.md`.
2. **Commit** the homework + progress + queue/misconception edits (learner said **commit OK, no push**).
   Suggested: `add: Week1 D5 context-managed logger package (logkit) + 06-29 retrieval/grading`.
3. **Day 6** — first day built under the **video-teacher-first** order: `LESSON.interactive.html` FIRST
   (embeds ≥1 predict-before-reveal + ≥1 quiz), then textbook md → homework/recall/solutions → learner `.py`.
   3 depth passes + 3 examples easy→hard per concept. See memory `teach-video-teacher-model` + project CLAUDE.md.
4. **Week-1 done →** produce `week01/CHEATSHEET.html` (printable, one-page, EN+中文, D1–D6).

## Watch-fors / discipline
- **Env = `base`** (Python 3.13.9, pytest 8.4.2): `conda run -n base python -m pytest <path> -v`.
- **Gate before grading any homework:** `bash .claude/skills/grade-day/run_checks.sh <day> [--gate-tests]`
  (`--gate-tests` when writing the tests IS the deliverable) + pyright DoD gate. Day not done until 0 errors + green.
- **Hint-ladder by default** — one nudge per turn, learner generates the code. Only hand over fixes if they
  explicitly ask (as they did this turn for logkit).
- **context7 = primary source** for any library lesson (NumPy / Week 2 onward) — cite primary URLs inline.
- Day-5 canonical traps now **M14–M18** (M18 = None-sentinel `or`/`bool()` clobbers falsy-but-valid).
- Two optional cleanups (non-blocking): flatten `Code_file/` → `day05_errors_context/logkit/`; strip the `//`
  comment in `pyrightconfig.json` (pyright CLI grumbles "unrecognized setting //").

## Suggested skills for the next session
- **`/teach`** — run Day-5 RECALL grading, then author Day 6 under the HTML-first order.
- **`grade-day`** — only if re-checking homework; RECALL itself is hint-ladder, not the gate.
- **`lesson-html-reviewer`** (subagent) + **frontend-design** skill — when building the Day-6 interactive HTML.
- **`/handoff`** — regenerate this doc at the next stopping point.

## Open questions for the learner (ask, don't assume)
- Run **RECALL now**, or pause here?
- **Commit** the session's work now (homework + progress + queue/misconceptions)? Still **no push**?
